export class Hospital {

    constructor(
        public name: string,
        public image: string,
        public user: string,
        // tslint:disable-next-line: variable-name
        public _id?: string
    ) { }

// tslint:disable-next-line: eofline
}